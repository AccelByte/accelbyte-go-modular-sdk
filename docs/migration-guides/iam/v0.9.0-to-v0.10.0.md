# [v0.10.0]

## BREAKING CHANGE

### Operations

The following operation(s) has been updated.

- Operation `AdminGetRoleOverrideConfigV3`
    - HTTP 400 response removed. The struct `AdminGetRoleOverrideConfigV3BadRequest` and the `Error400` field
      in `AdminGetRoleOverrideConfigV3Response` no longer exist. Code that references either of these will not
      compile.

### Models

The following model(s) has been updated.

- Model `ClientmodelClientCreationV3Request`
    - Field `ParentNamespace` has been removed.

- Model `ModelSSOPlatformCredentialRequest`
    - Field `IsActive` type changed from `*bool` (required pointer) to `bool` (value). Any code that
      dereferences the pointer or assigns a `*bool` to this field must be updated.

      ```diff
      - IsActive *bool `json:"isActive"`
      + IsActive bool  `json:"isActive"`
      ```

- Model `ModelThirdPartyLoginPlatformCredentialRequest`
    - Field `IsActive` type changed from `*bool` (required pointer) to `bool` (value). Any code that
      dereferences the pointer or assigns a `*bool` to this field must be updated.

      ```diff
      - IsActive *bool `json:"IsActive"`
      + IsActive bool  `json:"IsActive"`
      ```

[v0.10.0]: https://github.com/AccelByte/accelbyte-go-modular-sdk/compare/iam-sdk/v0.9.0..iam-sdk/v0.10.0
